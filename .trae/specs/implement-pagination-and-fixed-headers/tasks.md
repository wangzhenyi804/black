# 任务列表

- [x] 任务 1：创建/优化全局分页组件 `Pagination.tsx`: 实现符合行业标准的分页组件，支持当前页、总条数、每页条数切换和快速跳转。
- [x] 任务 2：重构页面布局以支持固定区域: 修改 `Layout.tsx` 或具体页面，实现主内容区域的分层布局（头部固定 + 内容滚动）。
    - [x] 子任务 2.1：修改 `Layout.tsx` 确保 `main` 容器支持 `flex` 布局且不产生全局滚动。
    - [x] 子任务 2.2：修改 `Media.tsx` 的布局，实现筛选区域固定和表格头部 `sticky`。
    - [x] 子任务 2.3：修改 `CodeSlots.tsx` 的布局，实现筛选区域固定和表格头部 `sticky`。
    - [x] 子任务 2.4：修改 `Users.tsx` 的布局，实现筛选区域固定和表格头部 `sticky`。
    - [x] 子任务 2.5：修改 `DataOverview.tsx` 的布局，实现筛选区域固定和表格头部 `sticky`。
    - [x] 子任务 2.6：修改 `CodeSlotData.tsx` 的布局，实现筛选区域固定和表格头部 `sticky`。
- [x] 任务 3：验证各页面的分页逻辑和固定效果: 确保在各种窗口尺寸下，固定头部和表头均正常显示，且分页逻辑无误。

# 任务依赖关系
- 任务 2 和 任务 3 依赖于 任务 1
