server:
  port: 9203

spring:
  datasource:
    url: jdbc:mysql://localhost:3616/blackad?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Zxc12300
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      # data-locations: classpath:data.sql, classpath:sql/update_codeslot_table.sql
      # Uncomment and update the above line to include your migration scripts during deployment
      # For example: data-locations: classpath:data.sql, file:/path/to/your/migration.sql
  data:
    redis:
      host: localhost
      port: 6979
  jackson:
    property-naming-strategy: SNAKE_CASE
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  mybatis-plus:
    configuration:
      map-underscore-to-camel-case: true
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000 # 1 day
